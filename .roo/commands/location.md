# Create Location Module

**Skill:** `docs/skills/entity_template/entity_with_index_and_audit.md`

**Module:** `location`

**Project:** `business-core`

**Core Data:** `business-core/business-core-db/src/models/person/location.rs`

**Description:**

This command creates the `location` module within the `business-core` project. It utilizes the `entity_with_index_and_audit` skill to generate the necessary components for the entity, including indexing and auditing features. The data model for the location entity is sourced from `business-core/business-core-db/src/models/person/location.rs`.